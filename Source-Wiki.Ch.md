# Source Wiki

一种用 LLM 构建结构化代码知识库的模式。

这是一份想法文件。把它复制给你的 LLM Agent（Claude Code、Codex、Cursor 等），让 Agent 在协作中补全具体实现。它的目标是传达核心模式——Agent 会根据你的项目实例化出合适的版本。

## 核心想法

大多数代码理解工具的工作方式是检索：你提问，工具返回相关文件，你自己拼凑答案。IDE 搜索、grep、甚至 AI 代码对话——都是检索。知识在每次提问时重新推导，没有积累。

这里的想法不同。LLM 增量地构建和维护一个**结构化 wiki**，位于你和源码之间。不是一堆平铺的摘要——而是一个分层的、互连的知识库，有类型的页面、显式的关系、累积的设计决策。源码变更时，LLM 检测变更、追踪在 wiki 中的影响范围、更新受影响的页面。你提问时，LLM 先查 wiki，不够时才回溯源码。

代码与通用知识有三个本质差异，使这个模式格外有效：

- **结构可发现。** 导出、导入、类型、测试——代码本身揭示了自身架构。LLM 读签名和关系，不只是文本。
- **真相可验证。** 源码始终是权威的。当 wiki 与代码矛盾时，代码胜出。错误可检测、可修正。
- **变更可追踪。** Git 提供精确的变更检测——LLM 精确知道什么变了，可以追踪连锁影响。

Wiki 是代码库的**编译视图**。你读 wiki 而非导航代码。LLM 负责写入和维护。

## 知识模型

页面按四层层级组织。每层描述不同的粒度，从具体到抽象：

```
architecture  ← 从底层归纳产生的系统级模式
    ↑
flow          ← 多个能力如何协作以完成业务目标
    ↑
module        ← 实现视角的分组（不限于业务领域）
    ↑
feature       ← 有明确目的的最小能力单元
```

每种页面类型位于自己的目录（`features/`、`modules/`、`flows/`、`architectures/`）。类型由位置决定，不是元数据。

**Feature** 映射到源码文件。一个 feature 页面记录哪些源文件实现了它、它做什么、遵循什么设计约束。

**Module** 归组相关 feature。Module 是多对多组合——feature 可以属于多个 module。Module 不限于业务领域，可以是基础设施、横切关注点、或共享工具。

**Flow** 描述 feature 和 module 如何协作。一个 flow 捕获一个业务流程：哪些能力参与、以什么顺序、为什么。

**Architecture** 页面从底层归纳产生——系统级模式、API 文档、部署拓扑、编码约定。它们不重复 feature 或 module 的细节，而是综合提炼。

每个页面有 YAML frontmatter 存储结构化元数据：标题、时间戳、标签、关系（以 `[[类型/页面名]]` 双链格式引用）、guidelines、issues。Frontmatter 可被机器读取——LLM 能程序化查询，无需全文搜索即可实现结构化导航。

## 操作

**Init.** 从零全量分析。LLM 扫描源码结构（目录、导出、类型、测试名称——只读签名，不读实现），提出模块划分方案，请你确认。然后逐模块处理：读相关源码、创建 feature 和 module 页面并填写元数据。全部模块完成后，推断跨模块 flow 和 architecture 页面——同样需要你确认。产物是一个完整的 wiki 骨架。目标是 ~70% 准确率——结构正确，细节可以有误。你修细节；LLM 搭结构。

**Ingest.** 源码变更后的增量同步。LLM 用 `git diff` 精确检测哪些文件变了，查询 wiki 找到受影响的页面（`source` 字段包含变更文件的 feature），追踪间接影响（包含这些 feature 的 module、涉及这些 module 的 flow），逐一更新页面。新文件可能触发新 feature 页面；删除文件可能废弃 feature。每次变更都记入日志。快速且精确——不需要重新扫描整个代码库。

**Lint.** 定期健康检查，覆盖四个维度：
- *时效性*：feature 页面中的源码路径是否仍然存在？
- *覆盖度*：有没有孤立页面？未链接的概念？没有被任何 feature 覆盖的源文件？
- *完整性*：每个页面的 frontmatter 是否完整且格式正确？
- *一致性*：页面之间是否矛盾？Wiki 与源码是否一致？

Lint 是只读的——它报告发现并建议修复，但只自动应用安全的机械性修复。内容变更需要你确认。结构性变更只报告，由你决定。

**Query.** 基于 wiki 回答问题。LLM 搜索相关页面、阅读、综合出答案。如果 wiki 信息不足，沿 `source` 引用回溯到实际代码。关键洞察：**好的回答可以沉淀为新的 wiki 页面。** 一个跨模块分析、一个发现的模式、一个架构洞察——这些是有价值的，不应消失在聊天历史中。LLM 建议沉淀；你批准。

## Guidelines

这是让 wiki 随时间复利增长的核心特性。

Guidelines 是从源码中提取的简短设计决策，存储在页面 frontmatter 中。例如："所有实体都有 createdAt/updatedAt 字段"、"认证使用 OAuth2 加 token 轮换"、"订单与库存模块之间使用事件驱动通信"。

它们充当**护栏**。LLM 修改任何 wiki 页面时，先读该页面的 guidelines 并遵循。这意味着：

- **Init** 阶段，guidelines 从代码模式中提取，经用户确认。
- **Ingest** 阶段，代码变更揭示新设计决策时产生新 guideline；变更使旧 guideline 失效时触发更新。
- **Lint** 阶段，跨页面反复出现的问题被识别为缺失的 guideline，建议添加。

Guidelines 形成反馈闭环。Wiki 对项目实际架构的表达越来越准确，因为每个操作既消费又生产 guidelines。随着时间推移，guidelines 成为项目最接近显式架构决策记录的东西——但它们分布在相关的页面中，而不是锁在没人读的独立文档里。

## 索引与日志

两个特殊文件帮助导航 wiki：

**index.md** 是内容目录——每个页面列出链接和一行摘要。按类型组织。每次操作后更新。LLM 先读 index.md 定位相关页面，再深入阅读。在中等规模（数百页面）下效果良好。

**log.md** 是 append-only 时间线——记录什么时间发生了什么：init、ingest、lint。每条条目以可解析的日期前缀开头。日志还充当 git diff 锚点：LLM 找到最后一条日志的 commit hash，据此确定上次同步以来的变更。

## 为什么有效

理解代码库的枯燥部分不是阅读——而是整理。维护交叉引用、保持描述最新、注意到一个模块的变更影响另外三个模块的文档。代码 wiki 和通用 wiki 死于同样的原因：维护负担增长快于价值。

源码 wiki 比通用知识 wiki 有一个优势：真相源是机器可读且版本可控的。LLM 能验证描述与代码是否一致、精确检测变更、通过结构化元数据追踪影响。它可以在一次操作中更新 15 个页面，并且确信哪些需要更新、哪些不需要。

人的职责：审核模块划分、批准 flow 和 guideline、修正 LLM 搞错的细节、思考这一切意味着什么。LLM 的职责：其他一切。

设计按以下顺序优先：
- **结构正确 > 内容准确。** 一个模块和 feature 正确但描述有瑕疵的 wiki，远比描述完美但缺少模块的 wiki 有用。细节容易修；结构不容易。
- **可校正性 > 自洽性。** Wiki 应该方便人类浏览、定位和修改。不要为 LLM 的便利优化——为人类审阅者优化。
- **增量改进 > 一次完美。** Init 给骨架，Ingest 保更新，Lint 找问题，Query 填空白。每个循环让 wiki 更好。

## 说明

本文档描述的是模式，不是具体实现。具体的页面类型、frontmatter 字段、查询工具和操作细节取决于你的代码库、你的 LLM 和你的偏好。知识模型（feature/module/flow/architecture）对大多数项目是合理的默认值，但你可能需要不同的层级——微服务可能需要 service 和 endpoint 层；monorepo 可能需要 package 和 workspace 层。四个操作（init/ingest/lint/query）覆盖了生命周期，但你可以增加操作，如 migrate（schema 演进）或 export（生成 API 文档）。正确的用法是把这份文档分享给你的 LLM Agent，一起构建具体实现。
