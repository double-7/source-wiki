---
title: "开发规范"
type: architecture
created: 2026-04-11
updated: 2026-04-11
source: "项目配置文件、lint 规则、代码风格定义"
tags: [conventions]
related: []
guidelines: []
issues: []
---

# 开发规范

> 本页面属于架构级文档。汇总项目中的编码约定和开发标准。
> 这是从源码中提炼的实际做法，不是理想化的规范文档。发现不一致时应标注矛盾。
> 如果某个章节不适用于当前项目（如 CLI 工具没有 controller 层），直接删除该章节，而非留空表格。

## 代码风格

### 命名约定

| 元素 | 约定 | 示例 |
|------|------|------|
| 文件名 | — | `user-service.ts` |
| 类名 | — | `UserService` |
| 函数名 | — | `getUserById` |
| 常量 | — | `MAX_RETRY` |

### 格式化与 Lint

| 工具/规则 | 配置文件 | 关键规则 |
|-----------|----------|----------|
| —         | —        | —        |

## 项目结构约定

```
src/
├── controllers/    # 请求处理层
├── services/       # 业务逻辑层
├── models/         # 数据模型
└── ...
```

> 根据项目实际结构描述每个目录的职责和约定。

## 设计模式与架构约定

| 模式/规则 | 使用场景 | 说明 |
|-----------|----------|------|
| —         | —        | —    |

> 记录项目中反复使用的设计模式、分层规则和调用约束。

## 错误处理

| 类型 | 处理方式 | 示例 |
|------|----------|------|
| 参数校验错误 | — | — |
| 业务逻辑错误 | — | — |
| 外部服务错误 | — | — |

## 测试规范

| 测试类型 | 目录 | 运行命令 |
|----------|------|----------|
| 单元测试 | —    | —        |
| 集成测试 | —    | —        |

> 记录测试编写约定（文件命名、Mock 策略、测试数据管理等）。

## 其他约定

> 记录以上未覆盖的约定（如日志规范、Git 规范、API 设计规范、安全规范等）。不适用不要列出。

## 待统一的领域

> 标注源码中发现的不一致之处，这些地方尚未形成统一规范。
> 例如："错误处理在老模块使用 try-catch，新模块使用 Result 类型，尚未统一。"
