---
title: "{{模块名}}"
created: 2026-04-21T00:00:00Z
updated: 2026-04-21T00:00:00Z
tags: []
depends: []
features: []
---

# {{模块名}}

> 核心章节（概述、功能表、源码文件）必须包含。
> 扩展章节仅在源码有明确证据时添加，无证据不要创建。
> 证据判定：✅ 源码中存在对应方法/类/常量 ｜ ✅ 配置文件中存在对应字段 ｜ ❌ 仅凭命名推测 ｜ ❌ 从其他页面推断

## 概述

> 用 2-3 句话描述这个模块的分组依据和在系统中的角色。不重复下属 feature 的功能描述，仅说明为什么这些 feature 被归为一组。
> 模块是代码实现视角的归纳分组，聚合了一组相关的 feature。分组依据不限业务领域——可以是技术关注点、基础设施、横切功能、共享工具。

## 包含的功能（Features）

| Feature | 说明 |
|---------|------|
| [[features/feature-a]] | 一句话描述此功能 |
| [[features/feature-b]] | 一句话描述此功能 |

## 涉及的源码文件

| 文件/目录 | 角色 |
|-----------|------|
| `src/path/` | 主要实现目录 |
| `src/other.ts` | 辅助文件 |

> 列出实现此模块涉及的主要源码路径。注意：模块不等于目录，一个模块可能跨越多个目录。

## 模块间依赖

> 【扩展章节】有模块间依赖时添加，无依赖时不要创建此章节。

### 依赖（本模块使用了谁）

- [[modules/其他模块A]] — 用于什么目的
- `外部包名` — 用于什么目的

### 被依赖（谁使用了本模块）

- [[modules/其他模块B]] — 在什么场景下使用本模块

## 共享的数据模型

> 【扩展章节】此模块下多个 feature 共用的数据结构、类型定义等。无共享数据模型时不要创建此章节。

> 如果某个数据模型足够重要且复杂，可在正文内展开描述。

## 配置

> 【扩展章节】模块相关的配置项（环境变量、配置文件字段等）。无模块特定配置时不要创建此章节。

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| —      | —      | — |

## 注意事项

> 【扩展章节】开发者需要注意的坑、限制、约定等。无特殊注意事项时不要创建此章节。
